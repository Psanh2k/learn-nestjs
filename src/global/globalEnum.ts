export enum HttpStatus {
    ERROR = 500,
    SUCCESS = 200,
    NOT_FOUND = 404,
}

export enum HttpMessage {
    ERROR = 'Server Internam Error',
    SUCCESS = 'Server Response Success',
    NOT_FOUND = 'Page Not Found',
}